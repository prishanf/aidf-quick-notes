# Moved: use the shared notes mockup

This per-feature mockup folder was retired to stop duplicating HTML/CSS/JS.

**Use instead:** [`docs/design/mockups/notes/`](../notes/)

```bash
npm run mockup:serve
# → http://127.0.0.1:3333/
```

See [`notes/README.md`](../notes/README.md) for why (AIDF scaffold-per-feature vs project DRY convention).
