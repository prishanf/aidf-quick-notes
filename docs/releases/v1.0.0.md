---
type: release-notes
track: B
required_when: "any change that ships user-visible behavior"
version: "v1.0.0"
git_tag: "v1.0.0"
source_revision: a0a5010a0cb6d0e079e13ba69012bf372d73dde2
date: 2026-07-29
status: approved
eligibility: production-approved
---

# AIDF Quick Notes v1.0.0

Second integrated release from `develop`: edit a note (PR #3) and Markdown-formatted note bodies (PR #4), on top of v0.1.0's create/list/delete.

**Production approval:** granted by prishanf (2026-07-29) — explicit instruction to cut this release to `main` as v1.0.0. Feature 4 UI QA sign-off produced and approved (`docs/qa/004-markdown-note-body-ui-signoff.md`) before this authorization.

## Highlights

- Notes can be edited in place from the home-page list, with title and/or body updated independently.
- Note bodies support Markdown — headings, bold/italic, links, lists, code, and blockquotes render as formatted HTML everywhere a body is shown, with a Write/Preview toggle on both the create and edit forms.
- Raw HTML or script typed into a body is always escaped to visible text, never executed; unsafe-scheme links (`javascript:`, `data:`) never become clickable — verified by both automated tests and independent AI review.

## Added

- Edit a note: inline row edit on the home page, `PATCH /api/notes/:id` with partial title/body updates, `updatedAt` advanced on save, `createdAt` unchanged.
- Markdown-formatted note body: safe CommonMark subset rendering (`app/utils/markdown.ts`, `markdown-it` with `html: false`) everywhere a body is shown; Write/Preview toggle on the create form and the inline edit form.

## Changed

- Upgraded vendored AIDF to 5.1.1: design agents now extend one shared mockup package per screen instead of copying scaffolding per feature.
- Design mockups consolidated: create/list/edit/delete/Markdown-preview now live in one shared `docs/design/mockups/notes/` package (token layer symlinked to the app); the per-feature scaffold copies from Features 1–3 were retired.

## Fixed

- Concurrent-delete during edit: saving an edit on a note that another caller deleted in the meantime now shows a not-found alert instead of silently failing (`371a7ba`, part of Feature 3).

## Upgrade notes

### Migration

None. Neither Feature 3 (edit) nor Feature 4 (Markdown body) changes the `notes` schema — both write to the same `title`, `body`, `updated_at` columns established in v0.1.0. Existing SQLite files need no new migration beyond what v0.1.0 already required (`npm run db:migrate`).

### Compatibility

- Same runtime shape as v0.1.0: local single-process Nuxt/Nitro, SQLite file at `SQLITE_PATH` (default `./data/notes.sqlite`), no authentication.
- **Behavior change to review before upgrading:** because Feature 4 has no stored plain-text/Markdown flag, **existing notes may render differently** once this ships if their body happens to contain Markdown-significant characters (a line starting with `#`, `*like this*`, backticks, a leading `-`, etc.). No data is rewritten — this is a render-time interpretation change only. See ADR 0003.
- New runtime dependency: `markdown-it@14.3.0` (MIT) + `@types/markdown-it` (dev). No new network calls, no new vulnerabilities per `npm audit`.

### Rollback

| Layer | Procedure |
|---|---|
| App / API / UI | Redeploy or checkout the prior `main` revision (`d62b6b1`, v0.1.0). Stops serving the edit UI, `PATCH` route, and Markdown rendering. |
| Schema | No migration in this release; nothing to roll back at the data layer. |
| Data | `title`/`body` are unchanged in shape; a rollback to v0.1.0 code still reads v1.0.0-written rows correctly (same columns, same types). |

## Eligibility (release gates)

Automated PR checks are CI-corroborated (`runner: ci`). Human/production gates below remain open — this artifact does **not** authorize production.

| Gate | Feature 3 (PR #3, edit) | Feature 4 (PR #4, Markdown) | Notes |
|---|---|---|---|
| CI evidence `runner: ci` | pass — [run 30424869722](https://github.com/prishanf/aidf-quick-notes/actions/runs/30424869722) | pass — [run 30429418995](https://github.com/prishanf/aidf-quick-notes/actions/runs/30429418995) (final tip, after AI-review remediation) | |
| Format/lint/typecheck/test/build | pass (CI) | pass (CI) | |
| AI review + ready-for-human comment | posted — `docs/reviews/003-edit-note.md` | posted — `docs/reviews/004-markdown-note-body.md` (one P1 found and fixed pre-merge) | |
| UI QA sign-off | approved — `docs/qa/003-edit-note-ui-signoff.md` | approved — `docs/qa/004-markdown-note-body-ui-signoff.md` | Both produced before production approval |
| GitHub PR approval | merged (COMMENTED-type AI review only; branch protection still advisory) | merged (same) | Same posture as v0.1.0 |
| Specialist / Track C gates | n/a — Track B | n/a — Track B | Neither carries a Track-C-forcing tag |
| Production approval | **granted** — prishanf, 2026-07-29 | same | |
| Recovery point before cut | recommended | recommended | No schema change, but copy the SQLite file if any real data exists |

**Verdict:** production approval granted by maintainer for merge to `main`. No open gaps.

## Durable documentation decisions

| Question | Answer | Artifact |
|---|---|---|
| Stable boundary / topology? | Unchanged single-process Nuxt → SQLite | `docs/architecture/overview.md` |
| Data model? | Unchanged — no new columns/tables in this release | `docs/architecture/data-model.md` |
| Integration contract? | `PATCH /api/notes/:id` added (Feature 3); no contract change from Feature 4 | `docs/api/notes.md` |
| Security posture? | New ADR: Markdown renders a safe subset only, raw HTML always escaped, no sanitizer needed while that holds | `docs/decisions/0003-markdown-safe-subset-no-raw-html.md` |
| Operator workflow? | No new steps; Markdown rendering is transparent to the operator | `docs/wiki/local-operator-runbook.md` |

## Contributors and references

- PR #3: https://github.com/prishanf/aidf-quick-notes/pull/3 — edit a note
- PR #4: https://github.com/prishanf/aidf-quick-notes/pull/4 — Markdown-formatted note body
- Specs: `docs/specs/003-edit-note.md`, `docs/specs/004-markdown-note-body.md`
- Plans: `docs/plans/003-edit-note.md`, `docs/plans/004-markdown-note-body.md`
- Reviews: `docs/reviews/003-edit-note.md`, `docs/reviews/004-markdown-note-body.md`
- ADR: `docs/decisions/0003-markdown-safe-subset-no-raw-html.md`
- Source tip on `develop`: `a0a5010`

## Next human / CI gate

1. Production cut: fast-forward/merge `main` to the `develop` tip, create annotated tag `v1.0.0` on that tip, push the tag.
2. Deployment record: `docs/deployments/v1.0.0.md` — complete after the cut with the actual tip, smoke results, and observation window.
