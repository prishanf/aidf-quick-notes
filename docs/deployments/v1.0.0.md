---
type: deployment-record
track: C
required_when: "every production release (`release` tag)"
status: draft
owner: prishanf
environment: production
source-revision: a0a5010a0cb6d0e079e13ba69012bf372d73dde2
git_tag: "v1.0.0"
release: docs/releases/v1.0.0.md
---

# Deployment record: v1.0.0

Not yet cut. Prepared ahead of the `develop` → `main` promotion pending production approval and the open Feature 4 UI QA gap noted in the release notes.

## Target

- Environment: production branch `main`
- Source revision: `a0a5010` (develop tip — PR #3 + PR #4 merged)
- Git tag: `v1.0.0` (not yet created — annotated tag to be pushed at cut time)
- Deployment/build ID: not yet run
- Change links: `docs/releases/v1.0.0.md`, PR #3, PR #4

## Approvals and checks

- PR approval: Features merged to `develop` via PR #3/#4 (COMMENTED-type AI review only; branch protection still advisory, same posture as v0.1.0)
- QA sign-off: Feature 3 approved (`docs/qa/003-edit-note-ui-signoff.md`); Feature 4 **missing** — open gap, needs an explicit accept-as-follow-up or a sign-off doc before cut
- Production approval: **pending**
- Checks: CI `runner: ci` on PR #3 (run 30424869722) and PR #4 (run 30429418995) — see release notes

## State changes

- Migration plan: none — no schema change in this release
- Recovery point: local SQLite only; copy the file before cutting if any data must be retained
- Configuration/feature flags: none

## Verification and observation

- Smoke checks: not yet run on `main` — planned: `npm ci && npm run db:migrate && npm test` on the cut tip
- Observation window / owner: prishanf (to be confirmed at cut time)
- Rollback trigger: migrate/data failure, or create/list/edit/delete/Markdown-render broken in smoke
- Rollback or forward-fix action: see release notes Rollback section

## Result

- Status: `follow-up-required` (Feature 4 UI QA gap; production approval not yet granted)
- Completed: not yet
- Notes: Update this record after the cut with the actual tag push, smoke results, and observation window.
