---
type: deployment-record
track: C
required_when: "every production release (`release` tag)"
status: in-progress
owner: prishanf
environment: production
source-revision: 8fd51f7188dd8d204debf4d4fe18e17e31d4f67f
release: docs/releases/v0.1.0.md
---

# Deployment record: v0.1.0

Git production branch cut: merge `develop` (v0.1.0) → `main` under maintainer production approval. No separate hosting deploy yet (local/`main` is the production revision).

## Target

- Environment: production branch `main`
- Source revision: merge tip after develop → main
- Deployment/build ID: git push to `origin/main`
- Change links: `docs/releases/v0.1.0.md`, PR #1, PR #2

## Approvals and checks

- PR approval: Features merged to develop via PR #1/#2; production cut authorized by prishanf
- QA sign-off: Feature 1 approved; Feature 2 accepted as follow-up
- Production approval: **granted** by prishanf (2026-07-28)
- Checks: CI `runner: ci` on PR #1 and #2 — see release notes

## State changes

- Migration plan: `docs/migrations/001-create-notes.md`
- Recovery point: local SQLite only; copy file before migrating any retained data
- Configuration/feature flags: none

## Verification and observation

- Smoke checks: run `npm ci && npm run db:migrate && npm test` on the `main` tip after merge
- Observation window / owner: prishanf
- Rollback trigger: migrate/data failure, or create/list/delete broken in smoke
- Rollback or forward-fix action: see release notes Rollback section

## Result

- Status: `running` — merge/push in progress
- Completed: —
- Notes: Update to `healthy` after `main` push succeeds and smoke is green.
