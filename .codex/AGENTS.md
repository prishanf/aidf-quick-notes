# Codex adapter

Follow the adapter-neutral rules in [AGENTS.md](../AGENTS.md). The command
contracts live in [.aidf/commands/](../.aidf/commands/); reference the one matching
your role by path in the task prompt.

`.aidf/` is the vendored framework: read it, never edit it.
